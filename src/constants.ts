import { RecordType } from "./types";

export const SIMILARITY_THRESHOLD = 0.75; // defined empirically
export const CONTENT_TYPES: RecordType[] = [
  "chart",
  "insight",
  "gdoc",
  "dod",
  "country",
];
